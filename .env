# Periodic Table of Information Primitives - Environment Configuration
# Copy or edit these values as needed. Values can be overridden per command.

# Embedding factorizer similarity threshold (default 0.3)
PERIODIC_SIM_THRESHOLD=0.3

# TF-IDF backoff similarity threshold (default 0.1)
PERIODIC_TFIDF_THRESHOLD=0.1

# Downstream classifier probability threshold (default 0.6)
PERIODIC_DS_THRESHOLD=0.6

# Internal algebra threshold for legacy paths (default 0.20)
PERIODIC_INTERNAL_THRESHOLD=0.20

# BabelNet API key (optional). Leave blank to disable BabelNet linking.
BN_KEY=b4ba3c67-1ec1-4c1d-bd2a-eb80918b0516
